using UnityEngine;
using System.Collections;

public class LivesText : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log(transform.guiText);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
